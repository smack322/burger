<ol>
    {{burger_name}}
    <button class="burger-devoured" data-id="{{id}}" data-newburger"{{burger}}">
    </button>
</ol>

{{!-- <li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button>
</li> --}}